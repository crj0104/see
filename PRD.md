# 分布式支付中台需求文档 (PRD)

## 1. 产品愿景与定位
本项目旨在打造一个**企业级、高可用、可插拔的分布式支付中台**。
它不是一个简单的业务系统后台，而是作为企业内部的基础设施，连接上游业务线（如电商、外卖、出行等商户系统）与下游第三方支付渠道（如微信、支付宝、Stripe等）。

核心目标是：**收敛支付复杂性、保证资金绝对安全、提供统一接入规范。**

## 2. 核心角色与用户旅程

| 角色 | 职责描述 |
| --- | --- |
| **业务方 (商户/Test Client)** | 接入支付中台，使用 API-Key 发起统一下单，接收中台的异步成功通知，完成自身业务的发货/履约。 |
| **支付中台 (Core)** | 负责鉴权、风控拦截、状态机流转、底层 SDK 交互、以及保证通知的最终一致性。 |
| **运营/财务 (Admin)** | 登录运营后台（Admin Service），为新业务方分配 API-Key，处理对账差错（长短款），以及在特殊情况下的强制退款或关单。 |
| **第三方渠道 (Provider)** | 微信/支付宝等，实际扣减用户资金，并通过外网向中台网关发送异步回调。 |

## 3. 核心业务痛点与架构解决方案

| 业务痛点 | 支付中台解决方案 |
| --- | --- |
| **接入繁琐与 SDK 冲突** | **渠道防腐层 (ACL)**：彻底剥离第三方 SDK 到独立的 `Channel Service`，业务线只需对接统一网关 API。 |
| **资金安全与并发覆盖** | **状态机与乐观锁**：严格的单向状态流转 + 数据库级乐观锁控制 (`UPDATE ... WHERE status = expected`)。 |
| **网络抖动与掉单** | **最终一致性**：`Outbox 本地消息表` 模式 + RabbitMQ 死信队列 + `Notification Service` 阶梯重试。 |
| **恶意攻击与刷单** | **风控引擎**：基于`责任链模式`的前置轻量级风控（IP黑名单、高频拦截、大额熔断）。 |
| **数据篡改与重放** | **全局鉴权**：API Key + HMAC-SHA256 签名 + Nonce 防重放（有效期内同一 Nonce 只能使用一次）。 |

## 4. 核心功能模块要求

### 4.1 统一交易模块 (Transaction)
*   **创建支付单**：接收上游业务的订单ID、金额、渠道，生成中台全局唯一的 `transaction_id`。
*   **执行支付**：RPC 调用底层渠道微服务，返回支付链接或前端唤起参数。
*   **前置风控拦截**：下单前经过责任链，一旦触发高频或黑名单规则，立即阻断。
*   **超时关单**：订单超过30分钟未支付，自动流转为 CLOSED，释放库存与幂等占用。

### 4.2 渠道适配模块 (Channel - 防腐层)
*   **SDK 隔离**：封装微信 V3、支付宝 RSA 的各种恶心加解密逻辑。
*   **原生回调解析**：被动接收外部原生回调，验签、解密后，转化为中台标准 JSON 并扔进 RabbitMQ 削峰。

### 4.3 统一回调与通知模块 (Notification)
*   **最终一致性投递**：交易服务修改完 MySQL 订单状态后，向 MQ 发送指令。通知服务消费指令并执行 HTTP POST 呼叫商户。
*   **阶梯重试**：如果商户服务器宕机或返回 500，执行指数退避重试（如 1m, 5m, 30m, 1h, 12h），确保商户 100% 收到钱款到账的通知。

### 4.4 运营与对账模块 (Admin & Reconciliation)
*   **商户配置**：为新接入的系统分配 API Key、配置渠道证书。
*   **T+1 自动对账**：定时拉取第三方支付渠道账单，与中台本地数据库流水比对。
*   **差异处理**：识别出“长款”、“短款”、“金额不一致”，并生成差异报告供财务人工干预平账。

## 5. 业务状态机流转规范
*   **支付状态 (PaymentStatus)**:
    `INIT` (初始化) -> `PENDING` (待支付) -> `PROCESSING` (渠道处理中) -> `SUCCESS` (成功) / `FAILED` (失败) / `TIMEOUT` (超时关闭)
*   **规则**：
    *   终态（SUCCESS, FAILED, TIMEOUT, CLOSED）不可逆。
    *   只有在 PENDING 或 RETRYING 状态下才能发起 PROCESSING。
